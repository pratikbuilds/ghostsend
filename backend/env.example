PORT=4000
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Optional. If unset: backend uses repo public/circuit2/ when present (shared with frontend), else backend node_modules (deploy).
# KEY_BASE_PATH=/path/to/circuit2/transaction2
